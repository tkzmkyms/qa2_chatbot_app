default: &default
  encoding: utf8
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  adapter: mysql2
  username: root
  password:
  host: localhost
  database: qa2_chatbot_app_development

test:
  <<: *default
  adapter: mysql2
  username: root
  password:
  host: localhost
  database: qa2_chatbot_app_test

production:
  <<: *default
  url: <%= ENV['DATABASE_URL'] %>

cable:
  url: <%= ENV['DATABASE_URL'] %>
